
interface UserProps {
  name: string;
  email: string;
  image: string;
}
export default function UserFooter({ user }: { user: UserProps }) {

  return (
    <div>
      <div>

      </div>
    </div>)
}